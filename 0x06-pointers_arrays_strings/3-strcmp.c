#include <string.h>
#include "main.h"

/**
 * _strcmp - compares two stringd s1 and s2
 * @s1: first string
 * @s2: second string
 * Return: success or failure
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
