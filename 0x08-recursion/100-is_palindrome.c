#include <string.h>
#include "main.h"

/**
 * is_palindrome - copies memory area
 * @s: pointer to memory to set
 * Return: factorial of n
 */

int is_palindrome(char *s)
{
	int len;

	len = strlen(s);

}
